'use strict';

module.exports = function(server, passport, auth) {

  /*
  // User routes
  var users = require('../controllers/users');

  server.get('/api/users', users.all);
  server.post('/api/users', users.create);
  server.get('/api/users/:id', users.one);
  server.get('/api/me', auth.requiresLogin, users.me);

  // Convenience
  server.post('/api/users/auth', passport.authenticate('local'), users.auth);

  // Autocomplete member call
  server.get('/api/members', auth.requiresLogin, users.searchMember);

  // Project routes
  var projects = require('../controllers/projects');

  server.post('/api/projects', auth.requiresLogin, projects.create);
  server.get('/api/projects', auth.requiresLogin, projects.all);
  server.get('/api/projects/:owner/:slug', auth.requiresLogin, projects.load);
  server.put('/api/projects/:owner/:slug', auth.requiresLogin, projects.update);

  // Task routes
  var tasks = require('../controllers/tasks');

  server.get('/api/:owner/:project/tasks', auth.requiresLogin, tasks.all);
  server.post('/api/:owner/:project/tasks', auth.requiresLogin, tasks.create);
  server.put('/api/:owner/:project/tasks/:task', auth.requiresLogin, tasks.update);

  // Team routes
  server.get('/api/:owner/:project/team', auth.requiresLogin, projects.team);
  server.put('/api/:owner/:project/team/:member', auth.requiresLogin, projects.updateTeam);
  server.post('/api/:owner/:project/team', auth.requiresLogin, projects.inviteTeamMember);

  // Stack routes
  var stacks = require('../controllers/stacks');

  server.get('/api/:owner/:project/stacks', auth.requiresLogin, stacks.all);
  server.get('/api/:owner/:project/stacks/:stack', auth.requiresLogin, stacks.load);
  server.post('/api/:owner/:project/stacks', auth.requiresLogin, stacks.create);
  server.put('/api/:owner/:project/stacks/:stack', auth.requiresLogin, stacks.update);
  */
  
};
